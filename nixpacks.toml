[phases.setup]
nixPkgs = ['python311']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install -r requirements.txt']

[start]
cmd = 'python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn leather_shop.wsgi:application --bind 0.0.0.0:$PORT'
